# Minimal declared deps for DocPilot V1 (Milestone A — not installed by this commit).
# Install later when implementing ingest/index/retrieve/generate.

# Search / NLP
rank-bm25>=0.2.2
numpy>=1.26.0

# Embeddings / vector store (choose one later; listed as intent)
# sentence-transformers>=3.0.0
# faiss-cpu>=1.8.0
# chromadb>=0.5.0

# LLM client
openai>=1.40.0

# CLI / config
python-dotenv>=1.0.0
typer>=0.12.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Dev / test
pytest>=8.0.0